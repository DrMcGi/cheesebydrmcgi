Revert target commit: d06934fb045a0f74f1f8e413e0e3564781a87f09

This repository is managed via this assistant.

Note: Automated revert requires checking out history or applying inverse patches, which is not available with current API-only file write tools.
Instead, we remove the SvelteKit-added files/paths referenced by that commit payload.
