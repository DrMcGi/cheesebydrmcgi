# Audio assets

Place `low-bass-drone.mp3` in this directory.

The landing page references it at:

- `/audio/low-bass-drone.mp3`

If the file is missing, the page will still load, but audio playback will fail silently.
